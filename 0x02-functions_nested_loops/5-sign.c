#include "main.h"
/**
 * print_sign - function
 * @n: parameter
 * Return: (0)
 */
int print_sign(int n)
{
	if (n > 0)
	{
		return (1);
	}
	else if (n == 0)
	{
		return (0);
	}
	else
	{
		return (-1);
	}
}
