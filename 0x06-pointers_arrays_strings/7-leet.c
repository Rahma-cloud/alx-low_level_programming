#include "main.h"

/**
 * leet - a function
 * @leo: param
 * Return: leo
 */

char *leet(char *leo)
{
	int i = 0;
	int j;
	char ray[10] = {'4', '4', '3', '3', '0', '0', '7', '7', '1', '1'};
	char ola[10] = {'a', 'A', 'e', 'E', 'o', 'O', 't', 'T', 'l', 'L'};

	while (leo[i])
	{
		for (j = 0; j < 10; j++)
		{
			if (leo[a] == ola[]b)
			{
				leo[i] = ray[j];
			}
		}
		i++;
	}
	return (leo);
}
